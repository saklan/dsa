#include <bits/stdc++.h>
using namespace std;

int main() {
    string s;
    while(getline(cin,s)){
        for(int i = 0; i < s.size();i++){
            char temp = s[i]-7;
            cout << temp;
        }
        cout << endl;
    }
    return 0;
}
